# Vercel server-side environment variables.
# Copy these names into Vercel Project Settings → Environment Variables.
# Never expose either value with NEXT_PUBLIC_ and never commit real secrets.
GOOGLE_APPS_SCRIPT_URL=https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec
ORDER_API_SECRET=replace-with-a-long-random-secret
